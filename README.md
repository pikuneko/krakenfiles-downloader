# 🚀 KrakenFiles 自動ダウンロード＆RAR自動解凍システム

ユーザーが入力した複数の KrakenFiles の URL から、広告やポップアップの妨害を物理的に排除しつつ自動でファイルをダウンロードし、完了後に一括で RAR ファイルを解凍する Python スクリプトです。

---

## 🛠️ 機能特徴

1. **ゾンビ連打＆広告強制クローズ機能**
   - クリック時に仕込まれた悪質な広告用別タブの自動検知・強制閉鎖。
   - ページ全体が広告に書き換えられた場合の自動「ページ戻り（Back）」と再試行ループ。
2. **ダウンロード完了シグナル検知**
   - バックグラウンド（`chrome://downloads`）を自動監視し、保存が 100% 終わるまで次の URL へ進まない安全設計。
3. **RAR 自動一括解凍**
   - 全ダウンロード終了後、PC 内の解凍ツール（7-Zip または WinRAR）を自動検出。
   - ファイル名と同名のフォルダを自動生成して綺麗に一括解凍。

---

## 📦 前提環境と準備

当システムを動かすには以下の環境が必要です。

* **OS**: Windows 10 / 11
* **Python**: バージョン 3.10 以上
* **Google Chrome**: 実装されている最新のブラウザ

### ⚡ クイック環境構築（推奨）
フォルダ内にある **`setup.bat`** をダブルクリックして実行してください。
自動的に管理者権限へ昇格し、以下の作業をワンクリックで一括代行します。
- `pip` の最新化
- 必要なライブラリ（`undetected-chromedriver`, `beautifulsoup4`, `requests`）の導入
- パソコン内に 7-Zip が無い場合、公式から最新版をバックグラウンド自動インストール

---

## 🚀 使い方・運用手順

### 1. 起動前のルール
- 普段お使いの通常の Google Chrome のウィンドウは、**すべて閉じた状態**（または競合しない状態）で実行することをおすすめします。

### 2. プログラムの実行
VS Codeのターミナル、またはコマンドプロンプトで以下のコマンドを実行します。

```bash
python main.py
```

### 3. URL の入力
1. 画面に `url1:` と表示されるので、KrakenFiles の URL を貼り付けて `Enter` を押します。
2. `url2:`, `url3:` と続けて、ダウンロードしたい数だけまとめて入力します。
3. **入力を終了する場合は、何も入力せずにそのまま `Enter` キーを押します。**

### 4. 実行中の対応
- 自動で特殊な Chrome ブラウザが立ち上がります。
- 画面に **Cloudflareの「人間であることを証明してください」**（パズルやチェックボックス）が表示された場合のみ、**制限時間内（12秒以内）に手動でチェックを入れて突破**してください。
- 認証通過後は、広告を弾きながら完全に自動で連打・ダウンロード・解凍まで処理が行われます。

---

## 🛑 トラブルシューティング

### Q1. 「session not created: This version of ChromeDriver only supports...」という赤いエラーが出る
- **原因**: ツール（`undetected-chromedriver`）が認識しているバージョンと、現在あなたのPCに入っている実際の Google Chrome のバージョンがズレています。
- **対策**: `main.py` の中にある `version_main=148` の数字部分を、現在エラーログに表示されている `Current browser version is 1xx.x.x` の主要な数字（3桁）に書き換えて上書き保存してください。

### Q2. 広告ページに飛ばされたまま進まない
- **原因**: 広告のスクリプトが想定以上に複雑な挙動をしています。
- **対策**: プログラムは最大 8 回まで「ページを戻して連打」を自動で繰り返します。もし数回ループしても戻れない場合は、ブラウザ画面の「←（戻るボタン）」を手動で一回押して元の KrakenFiles ページに戻してあげてください。

### Q3. ダウンロードが終わったのに解凍が始まらない
- **原因**: パソコンに「7-Zip」または「WinRAR」が標準の場所（`C:\Program Files`）にインストールされていません。
- **対策**: `setup.bat` を管理者として実行して 7-Zip を自動導入するか、公式サイトから手動で解凍ソフトをインストールしてください。
